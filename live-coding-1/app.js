// Escribe tu código aquí.
let palabra = "Hola";
const miArr = ["lápiz", "pluma", "goma", "sacapuntas"];

function miFuncion("Hola", miArr ) {
    for (let i = 0; i < miArr.length; i++ );
    if (miArr.length > palabra ) {
        return
    }
}